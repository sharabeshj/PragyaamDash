import { cardTitle } from "../../frontend";

const homepageCustomizeStyle = theme => ({
  cardIconTitle: {
    ...cardTitle,
    marginTop: "15px",
    marginBottom: "0px"
  }
});

export default homepageCustomizeStyle;
