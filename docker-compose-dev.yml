version: '3.6'

services:

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile-dev
    volumes:
      - './backend:/usr/src/app'
    ports:
      - "8000:8000"
    environment:
      - ENVIRONMENT=DEVELOPMENT